spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/unibuddy_dev
    username: postgres
    password: devpassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG

server:
  port: 8081
